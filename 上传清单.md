# 上传清单（GitHub 网页版）

> GitHub 网页版一次拖拽最多 100 个文件、单文件最大 25MB。
> 之前传不上去，是因为 `data/chelaile_raw/` 有 1411 个小文件（10.5MB）。
> 它只是抓取缓存，网站运行**不需要**，已加入 `.gitignore`，可以完全不上传。

## 需要上传的文件（29 个，共约 23MB）

### 第 1 批：网站本体（21 个文件）

按原有目录结构上传，路径和本地保持一致：

| 路径 | 说明 |
|---|---|
| `app.js` | 网站逻辑（含车来了修正层） |
| `index.html` | 网站入口（已加载修正层） |
| `transit_site/app.js` | OSM 版逻辑 |
| `transit_site/index.html` | OSM 版入口 |
| `transit_site/smoke_test.js` | 自动测试 |
| `data/bus_route_stops.js` | 线路停靠数据（已修复换台） |
| `data/stop_routes.js` | 站点→线路反查（已重建） |
| `data/derived_platforms.js` | 派生平台（已用车来了坐标更新） |
| `data/network_graph.js` | 线网图（已重建） |
| `data/chelaile_direction_stops.js` | 车来了方向分站坐标库（全量） |
| `data/chelaile_platform_fixes.js` | 显示修正层 |
| `transit_site/data/bus_route_stops.js` | 同上（OSM 版副本） |
| `transit_site/data/stop_routes.js` | 同上 |
| `transit_site/data/derived_platforms.js` | 同上 |
| `transit_site/data/network_graph.js` | 同上 |
| `transit_site/data/chelaile_platform_fixes.js` | 同上 |
| `.gitignore` | 已加入 raw 缓存忽略规则 |
| `scripts_fetch_chelaile.mjs` | 抓取脚本（可选） |
| `scripts_recheck_chelaile.mjs` | 复核脚本（可选） |
| `scripts_build_chelaile_library.mjs` | 坐标库构建（可选） |
| `scripts_apply_chelaile_fixes.mjs` | 修复脚本（可选） |

### 第 2 批：分析与报告（8 个文件）

| 路径 | 说明 |
|---|---|
| `output/network/chelaile_direction_stops.json` | 坐标库 JSON 版 |
| `output/network/chelaile_匹配统计.csv` | 匹配统计 |
| `output/network/chelaile_换台修复.csv` | 换台明细 |
| `output/network/chelaile_补台更新.csv` | 补台明细 |
| `output/network/线路状态核对_车来了.csv` | 状态核对总表 |
| `output/network/线路状态核对_疑似调整.csv` | 疑似调整清单 |
| `output/network/数据治理与设计决策记录.md` | 治理记录 |
| `scripts_status_report.mjs` | 状态报告脚本（可选） |

## 不用上传的

- `data/chelaile_raw/` —— 1411 个抓取缓存，需要时用 `scripts_fetch_chelaile.mjs` 重新生成；
- `output/network/backup_20260816_chelaile/` —— 本地备份；
- `scripts_probe_chelaile.mjs` / `scripts_compare_chelaile.mjs` / `scripts_chelaile_coverage.mjs` / `scripts_repair_chelaile.mjs` —— 临时探测脚本。

## 如果还是嫌麻烦：装 Git（强烈推荐，一劳永逸）

装好后在文件夹里右键 → "Git Bash Here"（或打开 PowerShell），输入三条命令即可全部上传，不用再数文件：

```text
git add -A
git commit -m "更新：车来了方向分站坐标库 + 状态核对"
git push
```

需要我帮你安装 Git 的话，说一声即可（一条命令的事）。

# 广州公交数据维护手册

> 本文档写给"未来的自己"：数据从哪来、怎么改、改完要重建什么、出事怎么回滚。
> 规则口径的决策背景见 `output/network/数据治理与设计决策记录.md`（本手册管"怎么做"，那份管"为什么"）。

---

## 一、数据来源

| 来源 | 说明 | 现状 |
|---|---|---|
| 广州公交/地铁 GIS（shapefile） | 2025 年采集：线路、站点、分段，字段含起讫/首末班/票价/运营公司/几何 | `guangzhou_bus_routes.shp/dbf`、`guangzhou_bus_stops*.shp/dbf`、`guangzhou_bus_segments.*` |
| 车来了（chelaile） | 第三方实时公交数据，提供线路上下行站点表 + 每个方向的 WGS84 分站坐标 + 分站编号 ①②③ | 抓取缓存 `data/chelaile_raw/`（1411 条线路基数），坐标库 `data/chelaile_direction_stops.js` |
| 高德开放平台 | 仅用于早期核对（站点 BV ID 与高德同源） | 无持续依赖 |

**站点 ID**：本库使用 BV 开头的高德站点 ID（如 `BV10016241`）；车来了分站编号（①/②/③）只用于方向分台判定，不写入 BV ID。

---

## 二、目录结构

```
根目录
├─ guangzhou_bus_*.shp/dbf        原始 GIS（不入库变更，.gitignore）
├─ _parsed_bus.json                原始解析缓存（不入库）
├─ web_data/                       由 shapefile 生成的网页原始数据（WGS84，不入库）
├─ data/                           网站数据（维护的主版本，入库）
│  ├─ bus_routes.js                线路（含几何、状态、status_note）
│  ├─ bus_stops.js                 站点（BV ID、坐标）
│  ├─ bus_route_stops.js           线路→有序停靠（换分台修正后的权威数据）
│  ├─ stop_routes.js               站点→途经线路
│  ├─ station_platforms.js         分站平台层（10162 平台→所属站/①②③编号/方向线路）
│  ├─ station_merge_map.js         站名写法差异合并表
│  ├─ chelaile_direction_stops.js  车来了方向分站坐标库（全量）
│  ├─ chelaile_platform_fixes.js   显示修正层（方向停靠点兜底坐标）
│  ├─ derived_platforms.js         派生方向站台（几何汇聚点）
│  ├─ stop_pinyin.js               站名拼音索引
│  ├─ network_graph.js             分站级线网图（分析用，网站不加载）
│  └─ metro_*.js                   地铁数据
├─ transit_site/                   网站本体（data/ 与根 data/ 保持同步）
├─ output/network/                 分析产物（CSV/报告/备份）
│  ├─ 分站规范化字典.csv           平台→站组归属（当前口径）
│  ├─ 线路状态核对_车来了.csv       线路状态分类
│  ├─ 线路问题汇总_待核对.csv       328 条问题线路汇总
│  └─ backup_*/                    各次改动前的备份
├─ *.mjs / *.js / *.py             脚本（见下）
├─ 数据维护手册.md                  本文档
└─ Git使用说明.md / README.md
```

> 根目录 `data/` 与 `transit_site/data/` 是同一份数据的两份拷贝，改数据必须两个都写。

---

## 三、数据管道总览

```
shapefile ──web_data_prep.py──▶ web_data/*.json
                                  │
                    make_site_data.py（WGS84） / make_site_data_gcj.py（GCJ02）
                                  ▼
                              data/*.js（初版）
                                  │
        此后所有修正都直接改 data/*.js（脚本），不再从 shapefile 重建
                                  ▼
  车来了抓取 ──▶ 坐标库 ──▶ 方向分台修正 ──▶ 平台层 ──▶ 网站
```

**重要**：`web_data_prep.py` 只能重建初版数据；当前 `data/*.js` 已包含大量修正（换台、补线、状态标记等），**不要用 web_data_prep.py 覆盖**，否则所有修正丢失。

---

## 四、脚本索引

### 4.1 原始预处理（一次性，一般不再跑）

| 脚本 | 作用 |
|---|---|
| `web_data_prep.py` | shapefile → web_data/*.json（初版） |
| `make_site_data.py` | web_data → data/*.js（WGS84） |
| `make_site_data_gcj.py` | web_data → data/*.js（GCJ02，适配高德底图；现由前端动态转换替代） |
| `make_stop_pinyin.py` | 生成 stop_pinyin.js（需 pypinyin） |

### 4.2 网络分析（读 data/，写 output/network/）

| 脚本 | 作用 |
|---|---|
| `make_network_analysis.js` | 共线/重复度/走廊 + 分站规范化字典（**改完站名/停靠后必须重跑**） |
| `make_network_graph.js` | 分站级线网图 network_graph.js（**改完停靠后必须重跑**） |
| `make_dir_platform_scan.js` | 方向分台候选扫描（换台/补台/剔除） |
| `make_duplicate_scan.js` | 疑似重复/变体线路排查 |
| `make_mismatch_report.js` | 停靠记录与几何不一致清单 |
| `make_network_xlsx.py` | CSV → Excel |
| `make_brt_*.py` / `analyze_*.py` / `diag_*.py` | BRT/527 专题分析、诊断（历史用途） |

### 4.3 车来了数据管线（新增线路/分站修正的主流程）

```text
scripts_fetch_chelaile.mjs        # 全量抓取线路上下行站点 → data/chelaile_raw/（可续跑）
scripts_recheck_chelaile.mjs      # 严格复核（精确线路名，纠正包含匹配污染）
scripts_fetch_lines_raw.mjs 110 113   # 为补充线路单独抓原始数据
scripts_build_chelaile_library.mjs    # 构建方向分站坐标库 data/chelaile_direction_stops.js
scripts_apply_chelaile_platforms.mjs  # 方向分台修正（按车来了坐标，[--apply] 生效）
scripts_build_station_platforms.mjs   # 构建分站平台层 data/station_platforms.js + 校验清单
scripts_status_report.mjs             # 线路状态分类（可核对/疑似调整/未收录）
scripts_mark_line_status.mjs          # 问题线路状态标记 + 汇总表
```

### 4.4 补充与修复（历史用途，改动已入库）

| 脚本 | 作用 |
|---|---|
| `scripts_add_missing_lines.mjs 101 113 …` | 补充源数据缺失的线路（101–119 段 11 条） |
| `scripts_fix_added_terminals.mjs` | 修正补充线路的终点/分站映射（可重复运行） |
| `scripts_update_changed_routes.mjs` | 用车来了最新走向更新已改线线路（41 条） |
| `scripts_apply_chelaile_fixes.mjs` | 换台/补台修复 + 显示修正层 |
| `scripts_build_station_merge_map.mjs` | 站名写法差异合并表 station_merge_map.js |
| `scripts_audit_station_families.mjs` | 同族站名排查（同方向共站=不同站判定） |
| `scripts_fix_cross_family_mismatch.mjs` | 修复跨族名字-ID 错配 |
| `scripts_show_adjusted.mjs` / `scripts_validate_station_merge.mjs` / `scripts_compare_chelaile.mjs` / `scripts_chelaile_coverage.mjs` | 排查/校验工具 |

---

## 五、核心数据文件说明

| 文件 | 字段/结构 | 备注 |
|---|---|---|
| `bus_routes.js` | FeatureCollection；`status`(1/0)、`status_label`(运营/疑似停运（待核对）/…)、`status_note`、起讫/首末班/票价/几何 | 状态标记由 `scripts_mark_line_status.mjs` 维护 |
| `bus_stops.js` | 站点 Feature，`stop_id`(BV…)、`name_cn`、坐标；`source:"chelaile"` 表示车来了新建站（CL 开头） | |
| `bus_route_stops.js` | `{ "线路(起--终)": [[序号, 站名, 站点ID], …] }` | **方向分台修正后的权威停靠表** |
| `station_platforms.js` | `STATION_PLATFORMS`：每平台 { id, name, stationId, stationName, chName, platformNo, lng, lat, lines } | 网站站详情/平台列表用 |
| `station_merge_map.js` | `STATION_MERGE_MAP`：变体站名→规范站名 | app.js 与 make_network_analysis.js 的 normStopName 套用 |
| `chelaile_platform_fixes.js` | 按"方向记录"的停靠点兜底坐标（显示层） | 懒加载 |
| `stop_pinyin.js` | 站名→拼音 | 懒加载 |

---

## 六、数据口径与规则（当前版本）

1. **站名规范化**：`normStopName` 先 `STATION_MERGE_MAP`、再去 公共汽车/BRT 前缀、数字/①-⑩ 后缀、方向后缀、括号别名；括号内容不同（开泰大道各路口）不合并。
2. **同名站距离聚类**：同规范名 + 坐标 ≤1000m 才视为同一站（跨区同名站拆开，南村 4 组）；临时站/招呼站按站点 ID 区分。
3. **方向分台**：以车来了坐标为准，每个方向停靠点 → 同一站族内距车来了坐标最近的平台（改善≥50m、目标≤300m）。
4. **总站规则**：总站与非总站不自动合并（陈家祠/陈家祠总站）。
5. **环线**：环线停靠列表去掉方向后缀、保留绕行重复站；同方向共站的两平台视为不同站。
6. **线路状态**：`status_label` 区分 运营 / 停运/停用 / 疑似停运（待核对）/ 疑似调整（待核对）；`status_note` 附说明。

---

## 七、改动后的重建清单

| 改了什么 | 必须重建/同步 |
|---|---|
| `bus_route_stops.js`（停靠/平台） | ①同步 transit_site；②`make_network_graph.js`；③`make_network_analysis.js`；④`scripts_build_station_platforms.mjs` |
| `bus_stops.js`（新增/移动平台） | 同上 + `make_stop_pinyin.py`（如需拼音） |
| `bus_routes.js`（状态/起讫/几何） | 同步 transit_site；如只改状态则无需重建其他 |
| `station_merge_map.js` | 同步 transit_site；重跑 `make_network_analysis.js` |
| `chelaile_*`（坐标库/修正层） | 同步 transit_site（懒加载文件） |
| `app.js` / `style.css` / `index.html` | 同步 transit_site |

**每次改完跑冒烟测试**：`node transit_site/smoke_test.js`（全绿再提交）。

---

## 八、备份与回滚

各次改动前都备份在 `output/network/backup_*`：

| 目录 | 对应改动 |
|---|---|
| `backup_20260816_chelaile` | 车来了坐标库/换台前 |
| `backup_20260816_route_updates` | 41 条改线线路更新前 |
| `backup_20260816_platform_fix` | 方向分台修正前 |

回滚：把备份文件复制回 `data/` 与 `transit_site/data/`，再按第七节重建依赖。

---

## 九、复现命令速查

```text
# 全量抓取车来了（首次或补录）
node scripts_fetch_chelaile.mjs
node scripts_recheck_chelaile.mjs

# 构建坐标库 + 方向分台修正（预演 → 应用）
node scripts_build_chelaile_library.mjs
node scripts_apply_chelaile_platforms.mjs          # 预演
node scripts_apply_chelaile_platforms.mjs --apply  # 应用

# 平台层 + 状态
node scripts_build_station_platforms.mjs
node scripts_status_report.mjs
node scripts_mark_line_status.mjs

# 网络分析 + 图
node make_network_analysis.js
node make_network_graph.js

# 冒烟测试
node transit_site/smoke_test.js
```

---

## 十、常见坑（速记）

- 不要用 `web_data_prep.py` 覆盖 data/（会丢全部修正）；
- 改数据必须同时改 `data/` 与 `transit_site/data/`；
- 车来了同一分站编号的坐标会随方向漂移（如国防大厦②有两个位置），判定平台用坐标而非编号；
- 括号内容不同（开泰大道(开达路口)/(科丰路口)）是不同站，别合并；
- 换台/换平台只应在同站族内进行（normKey 一致），否则会跨站污染（339 路的教训）；
- 环线（188/792/996）重复站是正常的，别去重；
- 详细决策背景见 `output/network/数据治理与设计决策记录.md`。

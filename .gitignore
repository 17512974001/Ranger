# 原始 GIS 数据：只作为“修改前”基线提交一次，之后不跟踪变更
*.shp
*.shx
*.dbf
*.prj
*.cpg

# 临时解析缓存
_parsed_bus.json

# 生成产物（可由脚本重新生成）
web_data/
brt_output/

# 车来了抓取缓存（1411 个小文件，可由 scripts_fetch_chelaile.mjs 重新生成；如需入库可合并为单文件）
data/chelaile_raw/

# 数据修复前备份（保留本地，不随仓库上传）
output/network/backup_*/

# 系统与编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/
__pycache__/
*.pyc
